# Show Profile
Finding a profile on Sharkey uses /api/users/show.
# Input
The payload input uses username & host (purpose unknown).
For example, a payload to search for nomaakip on sharkey.nomaakip.xyz would look like this:

`{username: "nomaakip", host: null}`